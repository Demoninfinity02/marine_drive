from django.urls import path
from . import views

# URL patterns for streaming app
urlpatterns = [
    # Placeholder for streaming endpoints
    path('live/', views.placeholder_view, name='streaming-live'),
]