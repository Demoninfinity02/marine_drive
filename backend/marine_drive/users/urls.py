from django.urls import path
from . import views

# URL patterns for users app
urlpatterns = [
    # Placeholder for user endpoints
    path('profile/', views.placeholder_view, name='user-profile'),
]